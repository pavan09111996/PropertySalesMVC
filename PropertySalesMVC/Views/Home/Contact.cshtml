@{
    ViewData["Title"] = "Contact Us";
}

@section Styles {
    <link rel="stylesheet" href="~/css/forms.css" asp-append-version="true" />
    <link rel="stylesheet" href="~/css/contact.css" asp-append-version="true" />
}
<section class="contact-page">
<div class="contact-wrapper">

    <!-- LEFT : INFO -->
    <div class="contact-info">
        <span class="eyebrow">Get In Touch</span>
        <h1>Let's talk property.</h1>
        <p class="subtitle">
            Contact Ronak Estate for property details, site visits,
            pricing and availability.
        </p>

        <div class="contact-details">
            <div class="contact-item">
                <div>
                    <label>Call Us</label>
                    <a href="tel:@ViewBag.Admin.Phone">@ViewBag.Admin.Phone</a>
                </div>
            </div>

            <div class="contact-item">
                <div>
                    <label>WhatsApp</label>
                    <a href="https://wa.me/@ViewBag.Admin.WhatsApp" target="_blank">
                        Chat on WhatsApp
                    </a>
                </div>
            </div>

            <div class="contact-item">
                <div>
                    <label>Office</label>
                    <span>Mumbai, Maharashtra</span>
                </div>
            </div>
        </div>
    </div>

    <!-- RIGHT : FORM -->
    <div class="contact-form-card">
        <h3>Send a Message</h3>

        <form method="post" asp-action="Contact" asp-controller="Home">
            <div class="field">
                <label>Your Name</label>
                <input type="text"
                       name="Name"
                       placeholder="Enter your name"
                       required />
            </div>

            <div class="field">
                <label>Phone Number</label>
                <input type="tel"
                       name="Phone"
                       placeholder="Enter phone number"
                       required />
            </div>

            <div class="field">
                <label>Message</label>
                <textarea name="Message"
                          rows="4"
                          placeholder="I am interested in a property..."
                          required></textarea>
            </div>

            <button type="submit" class="btn-brass-block">
                Send Message
            </button>

            <p class="form-note">
                We usually respond within a few hours.
            </p>

        </form>
    </div>

</div>
</section>